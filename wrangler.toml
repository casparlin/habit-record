# Bind D1 in the Cloudflare dashboard.
# Do not commit secrets. Put SECRET_ACCESS_TOKEN in Pages env vars.
name = "harbit_record"
compatibility_date = "2026-09-01"
pages_build_output_dir = "dist"

[[d1_databases]]
binding = "DB"
database_name = "habits"
database_id = "REPLACE_WITH_D1_DATABASE_ID"
