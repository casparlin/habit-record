# D1 is bound in Cloudflare Pages → Settings → Bindings.
# Do not put a fake database_id here; Pages will reject it on deploy.
name = "habit-record"
compatibility_date = "2026-09-01"
pages_build_output_dir = "dist"
